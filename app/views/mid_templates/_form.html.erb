<%= form_for(@mid_template) do |f| %>
  <% if @mid_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mid_template.errors.count, "error") %> prohibited this mid_template from being saved:</h2>

      <ul>
      <% @mid_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
